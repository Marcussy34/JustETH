type Store = 
 record {
   imageUrl: text;
   name: text;
   rating: nat;
 };
service : {
  createStore: (text, text) -> ();
  deleteStore: (text) -> (bool);
  getAllStores: () -> (vec Store) query;
  getStore: (text) -> (opt Store) query;
  updateStore: (text, nat, text) -> ();
}
